apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gitops-demo
  namespace: argocd
  finalizers:
    # Limpia los recursos del clÃºster cuando se borra la Application
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    # REEMPLAZA con la URL de tu repositorio
    repoURL: https://github.com/lasarmientogonzalez-spec/gitops-poc-arti4219.git
    targetRevision: main
    path: 01-crossplane          # Directorio donde estÃ¡n los manifiestos
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true       # Elimina recursos que se borraron de Git
      selfHeal: true    # Revierte cambios manuales no autorizados
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true   # Permite sync aunque haya CRDs aÃºn no instalados
